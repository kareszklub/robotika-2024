{
  "scripts": {
    "bundle": "esbuild --bundle index.js --outdir=static/dist --target=chrome80,firefox80 --minify --keep-names",
    "tailwind": "tailwindcss -i tailwind.css -o static/dist/style.css -m",
    "build": "pnpm bundle && pnpm tailwind"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.13",
    "daisyui": "^4.12.10",
    "esbuild": "^0.23.0",
    "htmx-ext-sse": "^2.2.1",
    "htmx.org": "^2.0.1",
    "tailwindcss": "^3.4.6"
  }
}
